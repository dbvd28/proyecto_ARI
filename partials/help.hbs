<section class="help">
  <h1>Como ayudarnos<span> / How to help us</span></h1>
 <section class="help-section">
    {{#each cards}}
      <div class="help-card">
        <div class="donation-images">
          <img src="{{imgSrc}}" alt="{{imgDSc}}" />
        </div>
        <h2>{{titlesp}} <span>/ {{titleeng}}</span></h2>
        <p class="donation-description">
          <strong>{{{dscSpa}}}</strong><br />
          {{{dcsEng}}}
        </p>
      </div>
    {{/each}}
  </section>
</section>